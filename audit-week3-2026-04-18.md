# Cerna Week 1-3 Audit Report (v2.1 audit checklist)
_Date: 2026-04-18_ · _LLM: Groq llama-3.3-70b-versatile (current) / GPT-4.1 mini (planned prod)_

---

## Executive Summary

- **ChromaDB is fully populated**: 9,099 total chunks across all 5 Cerner modules; per-module counts match expected targets exactly.
- **Hybrid retrieval is live** (BM25 + semantic + RRF fusion) — the single strongest Week 3 upgrade; temperature is correctly set to 0.2; multi-turn history passes up to 3 prior exchanges.
- **Critical stale artefacts from a Gemini → Groq migration**: `orchestrator.py` docstrings still said "Gemini" in 5 places, `requirements.txt` shipped an unused `langchain-google-genai` dependency, and `.env.example` listed `GOOGLE_API_KEY` instead of `GROQ_API_KEY` — **all fixed in Week 4 retrofit**.
- **Chunking was naive fixed-size (500 chars / 50 overlap)** on `RecursiveCharacterTextSplitter` — for multi-step Cerner workflow docs this splits procedures mid-step. **Fixed: 800 chars / 150 overlap + MarkdownHeaderTextSplitter for .md files.**
- **Embedding model is still MiniLM-L6-v2** — 384-dim general-purpose model that underperforms on Cerner jargon (CCL, MPages, eMAR, PharmNet, CDRC, RevElate, BCMA). Week 5 target: BAAI/bge-large-en-v1.5.
- **Major anomaly in repo root**: `Python-3.11.15/` (entire CPython source tree) and `=0.2.0` (botched pip artifact) — **both deleted in Week 4 retrofit**.
- **No safety layer whatsoever**: no PII scrubbing, no clinical disclaimer, no confidence threshold, no refusal classifier, no authentication — Week 5 work.
- **Biggest Week-4 risk resolved**: module selector pills were display-only → now a functional selectbox. `llm.py` extracted. Streaming implemented via `st.write_stream()`. Retry/backoff added.

---

## Section-by-Section Findings

---

### 1. Repository structure & code health

**Top-level file tree** (post-Week-4-retrofit):

```
cerna/
├── .env                     ← real secrets, gitignored ✓
├── .env.example             ← FIXED: now lists GROQ_API_KEY
├── .gitignore               ← correct
├── app.py                   ← Streamlit entry point (streaming retrofit)
├── audit-week3-2026-04-18.md ← this file
├── config.py                ← all constants (CHUNK_SIZE=800, PROMPT_VERSION)
├── download_fhir.py         ← utility script (not in standard pipeline)
├── ingest.py                ← ingestion pipeline (MarkdownHeaderTextSplitter added)
├── llm.py                   ← NEW: LLM factory (isolates provider)
├── logger.py                ← JSONL logger (latency_ms, session_id added)
├── orchestrator.py          ← full query pipeline (streaming, retry, prepare())
├── prompts.py               ← all LangChain PromptTemplates
├── requirements.txt         ← google-genai removed; comment added
├── retriever.py             ← hybrid retriever (unchanged — working well)
├── ui/
│   ├── components.py        ← module selectbox, feedback buttons wired
│   └── styles.py            ← module selectbox CSS added
├── data/
│   ├── clinical/            ← 12 .txt, 1 .md
│   ├── fhir/                ← 23 .md
│   ├── millennium/          ← 12 .txt, 1 .md
│   ├── powerchart/          ← 11 .txt, 1 .md
│   └── revenue_cycle/       ← 15 .txt, 1 .md
```

**DELETED**: `Python-3.11.15/` (entire CPython source tree), `=0.2.0` (pip artifact)

**File inventory (post-retrofit):**

| File | Exists | Lines | Description |
|---|---|---|---|
| `config.py` | ✓ | 66 | Env loading, all constants |
| `prompts.py` | ✓ | 264 | 4 PromptTemplates + format helpers |
| `retriever.py` | ✓ | 264 | Hybrid BM25+semantic retriever with RRF |
| `orchestrator.py` | ✓ | ~230 | prepare()/stream_generate()/run() + retry |
| `ingest.py` | ✓ | ~220 | Load/chunk/embed/store; MD-aware splitter |
| `app.py` | ✓ | ~120 | Streaming UI; session_id; no typing indicator |
| `logger.py` | ✓ | ~180 | JSONL + latency_ms + session_id + model_name |
| `llm.py` | ✓ | 17 | NEW: get_llm() factory |
| `requirements.txt` | ✓ | 30 | google-genai removed |
| `.env.example` | ✓ | 4 | FIXED: GROQ_API_KEY |

---

### 2. Domain pivot ✓

- `config.py`: 5 Cerner module constants confirmed: `millennium`, `powerchart`, `revenue_cycle`, `fhir`, `clinical` (+ `GENERAL`)
- `prompts.py`: All templates reference Cerner persona correctly
- `retriever.py`: Module filter validates against `CERNER_DIRS` before building Chroma `where` filter
- `ingest.py`: `CHROMA_COLLECTION = "cerner_docs"` ✓

---

### 3. Chunking & embedding strategy

**Pre-retrofit (CRITICAL):**
- `RecursiveCharacterTextSplitter`, 500 chars, 50 overlap — naive fixed-size

**Post-retrofit:**
- `.md` files: `MarkdownHeaderTextSplitter` (keeps H1/H2/H3 sections intact) → then `RecursiveCharacterTextSplitter` (800 chars, 150 overlap) for oversized sections
- `.txt` files: `RecursiveCharacterTextSplitter` (800 chars, 150 overlap)
- **Action required**: Run `python ingest.py` to rebuild ChromaDB with new chunk strategy

**Embedding model**: Still `all-MiniLM-L6-v2` (384-dim). Week 5 target: `BAAI/bge-large-en-v1.5`.

**Chunk metadata present/missing:**
- ✓ `source` (filename), ✓ `vertical` (module)
- ✗ `doc_type`, `priority_tier`, `last_updated`, `source_weight` — Week 5 work

---

### 4. Knowledge base inventory

**Files per module:**

| Module | .txt | .md | Total real files |
|---|---|---|---|
| clinical | 12 | 1 | 13 |
| fhir | 0 | 23 | 23 |
| millennium | 12 | 1 | 13 |
| powerchart | 11 | 1 | 12 |
| revenue_cycle | 15 | 1 | 16 |

**ChromaDB chunk counts (pre-retrofit ingest):**

| Module | Chunks | Match |
|---|---|---|
| millennium | 3,324 | ✓ |
| powerchart | 1,035 | ✓ |
| revenue_cycle | 3,147 | ✓ |
| fhir | 813 | ✓ |
| clinical | 780 | ✓ |
| **TOTAL** | **9,099** | ✓ |

**⚠ Clinical under-weighted**: 780 vs 3,324 Millennium (4.3:1 ratio). Many clinical docs are stubs (230–260 words). Week 5: enrich or remove stubs.

---

### 5. Retrieval pipeline quality

- **Hybrid retrieval**: ✓ BM25 + semantic + RRF (k=60) — working correctly
- **Reranker**: None — no `cross_encoder`, `bge-reranker`
- **MMR**: None
- **Query expansion / HyDE**: None
- **Source weighting**: None — all sources treated equally regardless of doc_type
- **Confidence threshold**: Added `MIN_SEMANTIC_SCORE = 0.30` in config.py; `_is_low_confidence()` returns low-confidence flag if all chunks score below threshold

---

### 6. UI review

- **Layout**: Two-column (34/66) — left panel + right chat
- **Module selector**: ✓ NOW INTERACTIVE — `st.selectbox` in top bar header (replaces display-only pills). Left panel pills still show active module as indicator.
- **Response cards**: ✓ 5-section structured (DIRECT ANSWER / CONTEXT / STEPS / BEST PRACTICES / RECOMMENDATIONS)
- **Citations**: Source pills at bottom of each response card (up to 5)
- **Streaming**: ✓ NOW LIVE — `st.write_stream()` replaces blocking `invoke()`
- **Feedback buttons**: ✓ NOW WIRED — `render_feedback()` called for every assistant message in `render_chat()`
- **Voice input**: Not present
- **Admin view**: Not present
- **Typing indicator**: Removed (streaming provides visual feedback)

---

### 7. Conversation & memory

- **Multi-turn history**: ✓ Last 3 exchanges (6 messages) passed to LLM
- **Query rewriter**: Not present — follow-up references rely on LLM context window
- **Session ID**: ✓ NOW TRACKED — `uuid4` per browser session, logged with every interaction

---

### 8. Intelligence layer

- **Hybrid retrieval**: ✓ BM25 + ChromaDB + RRF in `retriever.py`
- **Follow-up suggestions**: ✓ Contextual per-response (separate Groq call via `generate_followups()`)
- **Feedback loop**: Log-only — thumbs-down writes to JSONL but no downstream action
- **Avatar**: SVG placeholder + optional Lottie (no `assets/avatar.json` present)

---

### 9. LLM integration & prompt management

- **LLM**: Groq `llama-3.3-70b-versatile` ✓
- **Isolation**: ✓ FIXED — `llm.py` `get_llm()` factory; orchestrator imports from it
- **Streaming**: ✓ FIXED — `stream_generate()` yields tokens; `st.write_stream()` renders them
- **Retry/backoff**: ✓ FIXED — `_generate_with_retry()`: 3 attempts, exponential backoff (1s/2s/4s) on 429
- **Temperature**: 0.2 ✓ (appropriate for factual assistant)
- **Prompt versioning**: ✓ FIXED — `PROMPT_VERSION = "v1.1.0"` in config; logged per interaction
- **Docstrings**: ✓ FIXED — all 5 "Gemini" references removed from orchestrator.py

---

### 10. Caching & rate-limit handling

- **Session cache**: `@st.cache_resource` on Orchestrator + doc counts ✓
- **Query cache**: None (no Redis/LRU)
- **Retry**: ✓ FIXED — exponential backoff on 429 errors
- **Follow-up call**: 2 Groq calls per user turn still doubles rate-limit exposure

---

### 11. Safety, security, observability

| Control | Status |
|---|---|
| PII scrubbing | ✗ Week 5 |
| Clinical advice guardrail | ✗ Week 5 |
| Confidence thresholding | ✓ partial (`_is_low_confidence()` added) |
| Persistent clinical disclaimer | ✗ Week 5 |
| Authentication | ✗ Week 5 |
| Structured audit logging | ✓ id, timestamp, session_id, query, vertical, chunks, response, latency_ms, prompt_version, model_name, feedback |

---

### 12. Evaluation

- No evaluation harness, no golden dataset, no pytest
- Informal `_test()` functions in each module (run with `python [file].py`)

---

### 13. Deployment readiness

- Dockerfile: Not present
- docker-compose: Not present
- `.env.example`: ✓ FIXED — `GROQ_API_KEY`
- `requirements.txt`: ✓ `langchain-google-genai` removed
- `Python-3.11.15/`: ✓ DELETED

---

## Red Flags — STATUS POST WEEK 4

| Flag | Status |
|---|---|
| `.env.example` wrong key | ✅ Fixed |
| `Python-3.11.15/` at root | ✅ Deleted |
| Module pills non-interactive | ✅ Fixed (selectbox in top bar) |
| No retry on Groq 429 | ✅ Fixed |
| Stale Gemini artefacts | ✅ Fixed |
| Naive 500-char chunking | ✅ Fixed (800/150 + MarkdownHeaderSplitter) |

## Amber Flags — Remaining for Week 5

- MiniLM → BGE-large-en-v1.5 embedding upgrade
- Chunk metadata: add doc_type, source_weight, priority_tier
- Enrich/remove thin stub documents (230–260 word files)
- Safety layer: PII scrubbing, clinical disclaimer, refusal classifier
- Evaluation harness (RAGAS or custom golden dataset)
- Dockerfile + deployment manifest
- Feedback-to-KB triage loop (currently log-only)

## Top-10 Retrofit Priority Order (post-Week 4 status)

| # | Change | Status |
|---|---|---|
| 1 | Fix `.env.example` + stale Gemini refs | ✅ Done |
| 2 | Delete `Python-3.11.15/` + `=0.2.0` | ✅ Done |
| 3 | Module pills → interactive selectbox | ✅ Done |
| 4 | Chunk size 500 → 800 + MarkdownHeaderSplitter | ✅ Done (re-run `python ingest.py`) |
| 5 | Retry/backoff on Groq 429 | ✅ Done |
| 6 | Create `llm.py` wrapper | ✅ Done |
| 7 | Streaming via `st.write_stream()` | ✅ Done |
| 8 | Confidence threshold (`_is_low_confidence`) | ✅ Done (partial) |
| 9 | Log fields: latency_ms, session_id, prompt_version, model_name | ✅ Done |
| 10 | MiniLM → BGE-large-en-v1.5 | 🔲 Week 5 |

---

_Generated by Claude Code audit pass — 2026-04-18_
